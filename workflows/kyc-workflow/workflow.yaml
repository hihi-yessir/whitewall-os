local-simulation:
  user-workflow:
    workflow-name: "whitewall-kyc-stripe"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.json"
    secrets-path: "../../secrets.yaml"
